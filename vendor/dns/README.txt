Required configurations by this module. Create under /config/dns_config.bash
_DNS_SERVER_KEY - Key to access the local DNS server
_DNS_SERVER_IP - IP from the local DNS server
_DNS_TTL - TTL in seconds
